.side-bar
  .side-bar__user-name
    .side-bar__user-name__top-items
      .side-bar__user-name__top-items__name 
        = current_user.name
      .side-bar__user-name__top-items__icons
        = link_to new_group_path, class: "contents__btn contents__btn-edit" do
          = icon('fas','edit',  class: "contents__btn-icon")
        =link_to edit_user_path(current_user),class: "contents__btn contents__btn-cog" do
          =icon('fa','cog', class: "contents__btn-cog")
  .side-bar__group-list
    - current_user.groups.each do |group|
      .group
        = link_to group_messages_path(current_user), class: "list-box" do
          .list-box__group
            = group.name
          .list-box__latest-message
            まだメッセージがありません
    